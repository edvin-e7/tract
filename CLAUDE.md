<!-- agent-bootstrap:start -->
> ## For AI agents working in this repo
> If a local knowledge base is present at `~/.claude/knowledge/root-knowledge-tree.md`, read it
> first and follow it to the one domain note your task maps to before starting. Pull only the
> slice the task names — keep context focused, don't load everything. Harmless no-op if absent.
<!-- agent-bootstrap:end -->

